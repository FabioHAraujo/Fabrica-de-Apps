/*


DOCUMENTO DESTINADO UNICA E EXCLUSIVAMENTE PARA SALVAR TUDO QUE EU APRENDER


*/

/*

function t (){} // Utiizado par afunções

var a = "Declaração de variável, não são tipadas"
a = 12 // variáveis podem mudar de valor

console.log("Serve pra imprimir algo em console")

document.getElementById(id) // pega um elemento do html para ser editado por seu ID, geralmante usado assim:

var variavel = getElementById(pastel) 

//arrays: 
var lista = [1,165,"dada","pastel"]

lista.indexOf("pastel") // retorna a posição de pastel na lista

lista[2] // padrão né.

lista.indexOf("nada") // retorna -1 sempre que não existir na lista.

lista[posicao] = novoValor //altera o valor de uma posição

lista.push(valor) // adiciona um valor à lista

lista.shift(index) // tira da lista

lista.join(";") // muda o separador da lista

setInterval(acao, tempo(ms)) // Cria um repetidor mediante intervalo.

setTimeout(acao, tepoAteExecutar(ms)) // Cria um timeout

var = Declara Variavel global

let = Declara Variavel com escopo de bloco

const = let mas constante.

*/